services:
  mailhog:
    image: mailhog/mailhog:latest
    platform: linux/amd64
    container_name: susbonk-mailhog
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Web UI
    networks:
      - susbonk-network

networks:
  susbonk-network:
    external: true
